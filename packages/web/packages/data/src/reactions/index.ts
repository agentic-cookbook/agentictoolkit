// Public surface of the Reactions data domain — the client, its entity type, the row mapper,
// and the two folds (per-emoji tally, per-subject grouping) a consumer would otherwise rewrite.
export * from "./reactions";
