// Public surface of the Games data domain — the four CRUD clients for the
// operator-writable tables of the `game` schema, their entity types, and the two
// column crossings (nullable text, jsonb) those clients apply on the way past.
export * from "./games";
