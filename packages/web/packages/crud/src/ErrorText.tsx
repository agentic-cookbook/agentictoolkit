// ErrorText's home is @agentic-toolkit/ui (a generic error atom used across the
// platform). crud re-exports it because its CRUD surfaces render errors through it
// and it is part of crud's published API — crud's own components import it here.
export { ErrorText } from '@agentic-toolkit/ui/components/error-text'
