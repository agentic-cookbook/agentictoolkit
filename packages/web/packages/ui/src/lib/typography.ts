/** The uppercase-mono caption treatment — field captions, group titles, column
 *  headers, "quick reference" headings. One home so the caption look changes in
 *  exactly one place. */
export const fieldCaptionClass =
  "font-mono text-[0.7rem] uppercase tracking-wider text-apt-text-muted"
