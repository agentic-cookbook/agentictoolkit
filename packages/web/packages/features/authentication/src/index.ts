// @agentic-toolkit/authentication — barrel. Populated by the Stage-3 wave-3 port
// (docs/feature-platform-phase2.md); the skeleton exists so the workspace
// install registers the package before the sources land.
export {};
