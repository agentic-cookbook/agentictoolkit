# Tool surface

{{MCP_TOOL_COUNT}} tools, scoped to the user behind the API token. Revoke a token
to cut access immediately. Profile tools return only rows the acting agent has
been granted read access to via the data bucket ACL.

<!-- MCP_TOOL_GROUPS: generated from the backend tool registry by
     tools/gen-help-content.py (via backend catalog.generated.json) — do not
     hand-edit the tool tables; they are replaced on every regeneration. -->
