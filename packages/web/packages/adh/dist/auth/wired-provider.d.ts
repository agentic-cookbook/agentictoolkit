import { type ReactElement } from 'react';
import { type AuthProviderProps, type AuthUser } from '@agentic-toolkit/auth';
/** The toolkit AuthProvider with adh telemetry pre-wired (see above), plus the appearance sync. */
export declare function AuthProvider<U extends AuthUser = AuthUser>(props: AuthProviderProps<U>): ReactElement;
//# sourceMappingURL=wired-provider.d.ts.map