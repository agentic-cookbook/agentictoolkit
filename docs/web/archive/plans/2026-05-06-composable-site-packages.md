# Composable Site Packages — Implementation Plan (Phase 1 + 2)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Extract reusable site building blocks from `packages/reference-web-site/` into four sibling packages (`site-model`, `layout`, `controls`, `content`) so the same components can compose multiple sites with different content sources, routers, and themes. This plan covers Phase 1 (`site-model`) and Phase 2 (`layout`); Phases 3 (`controls`) and 4 (`content`) will be follow-up plans.

**Architecture:** Three layers. **Data model** = pure types and operations on `SiteEntry[]` / `SiteConfig` (no React). **View model** = thin React layer (providers, hooks, router/link abstraction) that adapts data to component-consumable state. **UI** = three component packages (layout/controls/content), each component dual-API (prop-driven for isolation; connected variant reads from view-model hooks). All component CSS uses `--color-*` / `--font-*` tokens defined in `packages/themes/styles/*.css`. The existing `packages/reference-web-site/` is **not modified** — new packages are net-new code, refactored copies from reference-web-site as a starting template.

**Tech Stack:** React 19, TypeScript, Vite, Tailwind CSS 4 (only inside `reference-web-site` and `site/` shell — new packages ship plain CSS), Vitest + @testing-library/react + jsdom, Fuse.js (for search).

**Constraints (user-stated):**
- Don't remove anything from `packages/reference-web-site/`.
- Each new export must have a runnable demo registered in `site/src/manifest.ts`.
- Each new component must be themable via the existing `packages/themes/` system (use `var(--color-*)` / `var(--font-*)` tokens; never literal colors; never Tailwind utility classes that bake in colors).

---

## File Structure

### `packages/site-model/` (Phase 1) — data + view model, no JSX

```
packages/site-model/
├── package.json                   # name, exports map, peerDeps
├── index.ts                       # public exports
├── types.ts                       # SiteEntry, NavNode, HeadingEntry, SiteConfig + sub-shapes
├── lib/
│   ├── nav.ts                     # buildNavTree(entries, sections) → NavNode[]
│   ├── breadcrumbs.ts             # slugToBreadcrumbs(slug) → BreadcrumbEntry[]
│   ├── search.ts                  # createSearchIndex / SearchIndex.query (Fuse.js wrapper)
│   ├── lookup.ts                  # findBySlug, getBySection, getByDomain (pure)
│   └── __tests__/                 # vitest unit tests for each lib file
├── providers/
│   ├── SiteConfigProvider.tsx     # provider + useSiteConfig hook
│   ├── ContentProvider.tsx        # provider + useContent hook (entries → derived nav/index)
│   ├── LinkProvider.tsx           # provider + useLink hook (router-agnostic <Link>)
│   ├── RouteProvider.tsx          # provider + useCurrentRoute hook (pathname/hash/navigate)
│   └── __tests__/
└── hooks/
    ├── useSearchState.ts          # search dialog state machine (separated from UI)
    └── __tests__/
```

**Note on Vite plugin:** The user requested abstraction. The build-time content loader (currently `packages/reference-web-site/vite-plugin/`) is a *producer* of `SiteEntry[]`. We will NOT move or duplicate it in this plan — `reference-web-site` keeps using its own copy untouched. A future task (separate plan) can publish a generic version under `packages/site-model/vite/`. For now, consumer sites either use the existing reference-web-site Vite plugin or supply `entries` themselves.

### `packages/layout/` (Phase 2) — structural rails, own CSS

```
packages/layout/
├── package.json                   # exports map for components + css
├── index.ts                       # exports all components + connected variants
├── components/
│   ├── Header.tsx                 # prop-driven
│   ├── HeaderConnected.tsx        # reads useSiteConfig + useLink, forwards to Header
│   ├── Sidebar.tsx                # prop-driven (nodes, currentPath, renderLink)
│   ├── SidebarConnected.tsx       # reads useContent + useCurrentRoute + useLink
│   ├── Breadcrumbs.tsx            # prop-driven (slug or trail)
│   ├── BreadcrumbsConnected.tsx   # reads useCurrentRoute, derives via slugToBreadcrumbs
│   ├── TableOfContents.tsx        # prop-driven (headings)
│   ├── TableOfContentsConnected.tsx  # reads useContent, derives current entry's headings
│   ├── AppShell.tsx               # composes header/sidebar/main/toc slots
│   └── __tests__/
└── css/
    ├── base.css                   # .awt-layout, shared structural styles
    ├── header.css                 # .awt-header
    ├── sidebar.css                # .awt-sidebar (+ tree, item, active)
    ├── breadcrumbs.css            # .awt-breadcrumbs
    ├── toc.css                    # .awt-toc
    └── app-shell.css              # .awt-app-shell (3-col grid layout)
```

**Phase 3 / Phase 4 sketches** appear at the end of this document. They are NOT covered by this plan.

---

## Phase 1: `packages/site-model/`

### Task 1.1: Add `site-model` to root exports map

**Files:**
- Modify: `package.json` (root)

- [ ] **Step 1: Edit root `package.json`**

In the `exports` object, add these entries (alphabetically grouped near the existing entries):

```json
"./site-model": "./packages/site-model/index.ts",
```

Place after the existing `"./reference-web-site/styles/tokens.css"` entry.

- [ ] **Step 2: Verify TypeScript still resolves**

Run: `cd site && npx tsc --noEmit`
Expected: PASS (we haven't created the file yet, but no consumer imports it yet either, so no failure)

- [ ] **Step 3: Commit**

```bash
git add package.json
git commit -m "chore: add site-model export entry to root package.json"
```

### Task 1.2: Create `site-model` package skeleton

**Files:**
- Create: `packages/site-model/package.json`
- Create: `packages/site-model/index.ts`
- Create: `packages/site-model/README.md`

- [ ] **Step 1: Create `packages/site-model/package.json`**

```json
{
  "name": "@agentic-cookbook/site-model",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./index.ts",
  "types": "./index.ts",
  "peerDependencies": {
    "react": "^19.0.0"
  },
  "dependencies": {
    "fuse.js": "^7.0.0"
  }
}
```

- [ ] **Step 2: Create `packages/site-model/index.ts` with placeholder export**

```ts
export {} // populated by subsequent tasks
```

- [ ] **Step 3: Create `packages/site-model/README.md`**

```markdown
# @agentic-cookbook/site-model

Pure data + view-model layer for composable Agentic Cookbook sites. Types,
operations on `SiteEntry[]` / `SiteConfig`, and React providers/hooks.
No JSX components, no CSS.

See `docs/superpowers/plans/2026-05-06-composable-site-packages.md`.
```

- [ ] **Step 4: Commit**

```bash
git add packages/site-model
git commit -m "feat(site-model): create package skeleton"
```

### Task 1.3: Port types from `reference-web-site`

**Files:**
- Create: `packages/site-model/types.ts`
- Modify: `packages/site-model/index.ts`

- [ ] **Step 1: Copy types.ts content**

Read `packages/reference-web-site/types.ts` and copy its full content verbatim into `packages/site-model/types.ts`.

- [ ] **Step 2: Re-export from `index.ts`**

Replace the placeholder content in `packages/site-model/index.ts` with:

```ts
export type {
  SiteConfig,
  BrandingConfig,
  MetaConfig,
  HeroConfig,
  NavConfig,
  NavSectionConfig,
  ExternalLink,
  ThemeConfig,
  FeaturesConfig,
  SlotsConfig,
  HeaderSlotProps,
  SiteEntry,
  SiteFrontmatter,
  HeadingEntry,
  NavNode,
} from './types'
```

- [ ] **Step 3: TypeScript check**

Run: `npx tsc --noEmit`
Expected: PASS

- [ ] **Step 4: Commit**

```bash
git add packages/site-model
git commit -m "feat(site-model): port types from reference-web-site"
```

### Task 1.4: Port + test `buildNavTree`

**Files:**
- Create: `packages/site-model/lib/nav.ts`
- Create: `packages/site-model/lib/__tests__/nav.test.ts`
- Modify: `packages/site-model/index.ts`

- [ ] **Step 1: Write failing test `packages/site-model/lib/__tests__/nav.test.ts`**

```ts
import { describe, it, expect } from 'vitest'
import { buildNavTree } from '../nav'
import type { SiteEntry, NavSectionConfig } from '../../types'

const entries: SiteEntry[] = [
  {
    slug: '/guides/install',
    section: 'guides',
    raw: '',
    html: '',
    headings: [],
    frontmatter: { title: 'Install' },
  },
  {
    slug: '/guides/configure',
    section: 'guides',
    raw: '',
    html: '',
    headings: [],
    frontmatter: { title: 'Configure' },
  },
  {
    slug: '/api/overview',
    section: 'api',
    raw: '',
    html: '',
    headings: [],
    frontmatter: { title: 'API Overview' },
  },
]

const sections: NavSectionConfig[] = [
  { key: 'guides', label: 'Guides', path: '/guides' },
  { key: 'api', label: 'API', path: '/api' },
]

describe('buildNavTree', () => {
  it('groups entries under their section nodes in declared order', () => {
    const tree = buildNavTree(entries, sections)
    expect(tree).toHaveLength(2)
    expect(tree[0]).toMatchObject({ label: 'Guides', path: '/guides' })
    expect(tree[1]).toMatchObject({ label: 'API', path: '/api' })
    expect(tree[0].children).toHaveLength(2)
    const guideLabels = tree[0].children.map((c) => c.label).sort()
    expect(guideLabels).toEqual(['Configure', 'Install'])
    expect(tree[1].children).toHaveLength(1)
    expect(tree[1].children[0]).toMatchObject({ label: 'API Overview', path: '/api/overview' })
  })

  it('returns declared sections even when they have no entries', () => {
    const tree = buildNavTree([], sections)
    expect(tree).toHaveLength(2)
    expect(tree[0].children).toEqual([])
  })

  it('creates an orphan section node for entries whose section is not declared', () => {
    const orphan: SiteEntry = {
      slug: '/misc/extra',
      section: 'misc',
      raw: '',
      html: '',
      headings: [],
      frontmatter: { title: 'Extra' },
    }
    const tree = buildNavTree([...entries, orphan], sections)
    const miscSection = tree.find((n) => n.path === '/misc')
    expect(miscSection).toBeDefined()
    expect(miscSection!.children).toHaveLength(1)
    expect(miscSection!.children[0].label).toBe('Extra')
  })

  it('skips the root entry "/"', () => {
    const root: SiteEntry = {
      slug: '/',
      section: 'guides',
      raw: '',
      html: '',
      headings: [],
      frontmatter: { title: 'Home' },
    }
    const tree = buildNavTree([root, ...entries], sections)
    expect(tree[0].children.find((c) => c.path === '/')).toBeUndefined()
  })
})
```

- [ ] **Step 2: Run test to confirm it fails**

Run: `npx vitest run packages/site-model/lib/__tests__/nav.test.ts`
Expected: FAIL with "Cannot find module '../nav'"

- [ ] **Step 3: Implement `packages/site-model/lib/nav.ts`**

Copy the contents of `packages/reference-web-site/lib/navigation.ts` verbatim into `packages/site-model/lib/nav.ts`. The implementation is already pure (parameter-driven) and matches the canonical `NavNode` shape `{ label, path, domain?, children: NavNode[] }`. The full file should look like:

```ts
import type { SiteEntry, NavNode, NavSectionConfig } from '../types'

function titleCase(s: string): string {
  return s
    .split('-')
    .map((w) => w.charAt(0).toUpperCase() + w.slice(1))
    .join(' ')
}

export function buildNavTree(
  entries: SiteEntry[],
  sections: NavSectionConfig[],
): NavNode[] {
  const sectionMap = new Map<string, NavNode>()
  const sectionOrder: string[] = []

  for (const section of sections) {
    sectionMap.set(section.key, {
      label: section.label,
      path: section.path,
      children: [],
    })
    sectionOrder.push(section.key)
  }

  for (const entry of entries) {
    if (!entry.section || entry.slug === '/') continue

    let sectionNode = sectionMap.get(entry.section)
    if (!sectionNode) {
      sectionNode = {
        label: titleCase(entry.section),
        path: '/' + entry.section,
        children: [],
      }
      sectionMap.set(entry.section, sectionNode)
      sectionOrder.push(entry.section)
    }

    const parts = entry.slug.split('/').filter(Boolean)
    const pathWithinSection = parts.slice(1)

    if (pathWithinSection.length === 0) {
      sectionNode.domain = entry.domain
      continue
    }

    let current = sectionNode
    for (let i = 0; i < pathWithinSection.length - 1; i++) {
      const segment = pathWithinSection[i]
      let child = current.children.find(
        (c) => c.label === titleCase(segment) && c.children.length > 0,
      )
      if (!child) {
        child = {
          label: titleCase(segment),
          path: '/' + parts.slice(0, i + 2).join('/'),
          children: [],
        }
        current.children.push(child)
      }
      current = child
    }

    current.children.push({
      label: entry.frontmatter.title,
      path: entry.slug,
      domain: entry.domain,
      children: [],
    })
  }

  function sortChildren(node: NavNode) {
    node.children.sort((a, b) => {
      if (a.children.length > 0 && b.children.length === 0) return -1
      if (a.children.length === 0 && b.children.length > 0) return 1
      return a.label.localeCompare(b.label)
    })
    for (const child of node.children) sortChildren(child)
  }

  const tree = sectionOrder
    .map((key) => sectionMap.get(key)!)
    .filter(Boolean)

  for (const node of tree) sortChildren(node)
  return tree
}
```

- [ ] **Step 4: Run test to confirm it passes**

Run: `npx vitest run packages/site-model/lib/__tests__/nav.test.ts`
Expected: PASS (3 tests)

- [ ] **Step 5: Re-export from `index.ts`**

Append to `packages/site-model/index.ts`:

```ts
export { buildNavTree } from './lib/nav'
```

- [ ] **Step 6: Commit**

```bash
git add packages/site-model
git commit -m "feat(site-model): buildNavTree with unit tests"
```

### Task 1.5: Port + test `slugToBreadcrumbs`

**Files:**
- Create: `packages/site-model/lib/breadcrumbs.ts`
- Create: `packages/site-model/lib/__tests__/breadcrumbs.test.ts`
- Modify: `packages/site-model/index.ts`

- [ ] **Step 1: Write failing test**

```ts
import { describe, it, expect } from 'vitest'
import { slugToBreadcrumbs } from '../breadcrumbs'

describe('slugToBreadcrumbs', () => {
  it('returns empty array for root', () => {
    expect(slugToBreadcrumbs('/')).toEqual([])
  })

  it('produces one breadcrumb per path segment, no Home prefix', () => {
    expect(slugToBreadcrumbs('/guides/install')).toEqual([
      { label: 'Guides', path: '/guides' },
      { label: 'Install', path: '/guides/install' },
    ])
  })

  it('title-cases dashed segments', () => {
    expect(slugToBreadcrumbs('/api/getting-started')[1]).toEqual({
      label: 'Getting Started',
      path: '/api/getting-started',
    })
  })
})
```

- [ ] **Step 2: Run test to confirm failure**

Run: `npx vitest run packages/site-model/lib/__tests__/breadcrumbs.test.ts`
Expected: FAIL "Cannot find module"

- [ ] **Step 3: Implement `packages/site-model/lib/breadcrumbs.ts`**

Copy the contents of `packages/reference-web-site/lib/domain-utils.ts` verbatim, plus a named type export. The function returns an empty array for `/` and does not include a "Home" prefix — consumers can prepend their own.

```ts
export type BreadcrumbEntry = { label: string; path: string }

function titleCase(s: string): string {
  return s
    .split('-')
    .map((w) => w.charAt(0).toUpperCase() + w.slice(1))
    .join(' ')
}

export function slugToBreadcrumbs(slug: string): BreadcrumbEntry[] {
  if (slug === '/') return []
  const parts = slug.split('/').filter(Boolean)
  const crumbs: BreadcrumbEntry[] = []
  for (let i = 0; i < parts.length; i++) {
    crumbs.push({
      label: titleCase(parts[i]),
      path: '/' + parts.slice(0, i + 1).join('/'),
    })
  }
  return crumbs
}
```

- [ ] **Step 4: Run test to confirm pass**

Run: `npx vitest run packages/site-model/lib/__tests__/breadcrumbs.test.ts`
Expected: PASS

- [ ] **Step 5: Export from index**

Append to `packages/site-model/index.ts`:

```ts
export { slugToBreadcrumbs, type BreadcrumbEntry } from './lib/breadcrumbs'
```

- [ ] **Step 6: Commit**

```bash
git add packages/site-model
git commit -m "feat(site-model): slugToBreadcrumbs with unit tests"
```

### Task 1.6: Port + test entry lookup helpers

**Files:**
- Create: `packages/site-model/lib/lookup.ts`
- Create: `packages/site-model/lib/__tests__/lookup.test.ts`
- Modify: `packages/site-model/index.ts`

- [ ] **Step 1: Write failing test**

```ts
import { describe, it, expect } from 'vitest'
import { findBySlug, getBySection, getByDomain } from '../lookup'
import type { SiteEntry } from '../../types'

const make = (overrides: Partial<SiteEntry> & { slug: string; section: string }): SiteEntry => ({
  raw: '',
  html: '',
  headings: [],
  frontmatter: { title: overrides.slug },
  ...overrides,
})

const entries: SiteEntry[] = [
  make({ slug: '/g/a', section: 'g', domain: 'd1', frontmatter: { title: 'A' } }),
  make({ slug: '/g/b', section: 'g', domain: 'd1', frontmatter: { title: 'B' } }),
  make({ slug: '/h/c', section: 'h', domain: 'd2', frontmatter: { title: 'C' } }),
]

describe('lookup helpers', () => {
  it('findBySlug returns the matching entry', () => {
    expect(findBySlug(entries, '/g/b')?.frontmatter.title).toBe('B')
  })

  it('findBySlug returns undefined for misses', () => {
    expect(findBySlug(entries, '/nope')).toBeUndefined()
  })

  it('getBySection returns all entries for a section in source order', () => {
    expect(getBySection(entries, 'g').map((e) => e.slug)).toEqual(['/g/a', '/g/b'])
  })

  it('getByDomain filters by domain field', () => {
    expect(getByDomain(entries, 'd2').map((e) => e.slug)).toEqual(['/h/c'])
  })
})
```

- [ ] **Step 2: Run to confirm failure**

Run: `npx vitest run packages/site-model/lib/__tests__/lookup.test.ts`
Expected: FAIL

- [ ] **Step 3: Implement `packages/site-model/lib/lookup.ts`**

```ts
import type { SiteEntry } from '../types'

export function findBySlug(entries: SiteEntry[], slug: string): SiteEntry | undefined {
  return entries.find((e) => e.slug === slug)
}

export function getBySection(entries: SiteEntry[], section: string): SiteEntry[] {
  return entries.filter((e) => e.section === section)
}

export function getByDomain(entries: SiteEntry[], domain: string): SiteEntry[] {
  return entries.filter((e) => e.domain === domain)
}
```

- [ ] **Step 4: Run to confirm pass**

Run: `npx vitest run packages/site-model/lib/__tests__/lookup.test.ts`
Expected: PASS

- [ ] **Step 5: Export from index**

Append:

```ts
export { findBySlug, getBySection, getByDomain } from './lib/lookup'
```

- [ ] **Step 6: Commit**

```bash
git add packages/site-model
git commit -m "feat(site-model): entry lookup helpers"
```

### Task 1.7: Port + test search index

**Files:**
- Create: `packages/site-model/lib/search.ts`
- Create: `packages/site-model/lib/__tests__/search.test.ts`
- Modify: `packages/site-model/index.ts`

- [ ] **Step 1: Write failing test**

```ts
import { describe, it, expect } from 'vitest'
import { createSearchIndex } from '../search'
import type { SiteEntry } from '../../types'

const entries: SiteEntry[] = [
  {
    slug: '/g/install',
    section: 'g',
    raw: '',
    html: '',
    headings: [],
    frontmatter: { title: 'Install Guide', summary: 'How to install' },
  },
  {
    slug: '/g/config',
    section: 'g',
    raw: '',
    html: '',
    headings: [],
    frontmatter: { title: 'Configuration', summary: 'Configure the app' },
  },
  {
    slug: '/api/auth',
    section: 'api',
    raw: '',
    html: '',
    headings: [],
    frontmatter: { title: 'Authentication', summary: 'API auth flows' },
  },
]

describe('createSearchIndex', () => {
  it('returns a SearchIndex with a query method', () => {
    const idx = createSearchIndex(entries)
    expect(typeof idx.query).toBe('function')
  })

  it('finds matches in frontmatter.title', () => {
    const results = createSearchIndex(entries).query('install')
    expect(results.map((r) => r.entry.slug)).toContain('/g/install')
  })

  it('finds matches in frontmatter.summary', () => {
    const results = createSearchIndex(entries).query('auth flows')
    expect(results[0]?.entry.slug).toBe('/api/auth')
  })

  it('returns empty array for empty query', () => {
    expect(createSearchIndex(entries).query('')).toEqual([])
  })
})
```

- [ ] **Step 2: Run to confirm failure**

Run: `npx vitest run packages/site-model/lib/__tests__/search.test.ts`
Expected: FAIL

- [ ] **Step 3: Implement `packages/site-model/lib/search.ts`**

Match the search keys used by reference-web-site (`frontmatter.title`, `frontmatter.summary`, `domain`) and same threshold so existing behavior is preserved. The new function returns a `SearchIndex` object (rather than a singleton + free function) so each consumer's index is isolated.

```ts
import Fuse from 'fuse.js'
import type { SiteEntry } from '../types'

export type SearchResult = {
  entry: SiteEntry
  score: number
}

export type SearchIndex = {
  query: (q: string) => SearchResult[]
}

export function createSearchIndex(entries: SiteEntry[]): SearchIndex {
  const fuse = new Fuse(entries, {
    keys: [
      { name: 'frontmatter.title', weight: 3 },
      { name: 'frontmatter.summary', weight: 2 },
      { name: 'domain', weight: 1 },
    ],
    threshold: 0.3,
    includeScore: true,
    minMatchCharLength: 2,
  })
  return {
    query(q: string): SearchResult[] {
      if (!q.trim()) return []
      return fuse.search(q).map((r) => ({ entry: r.item, score: r.score ?? 0 }))
    },
  }
}
```

- [ ] **Step 4: Run to confirm pass**

Run: `npx vitest run packages/site-model/lib/__tests__/search.test.ts`
Expected: PASS

- [ ] **Step 5: Export from index**

Append:

```ts
export { createSearchIndex, type SearchIndex, type SearchResult } from './lib/search'
```

- [ ] **Step 6: Commit**

```bash
git add packages/site-model
git commit -m "feat(site-model): createSearchIndex with unit tests"
```

### Task 1.8: SiteConfigProvider + useSiteConfig

**Files:**
- Create: `packages/site-model/providers/SiteConfigProvider.tsx`
- Create: `packages/site-model/providers/__tests__/SiteConfigProvider.test.tsx`
- Modify: `packages/site-model/index.ts`

- [ ] **Step 1: Write failing test**

```tsx
import { describe, it, expect } from 'vitest'
import { renderHook } from '@testing-library/react'
import { SiteConfigProvider, useSiteConfig } from '../SiteConfigProvider'
import type { SiteConfig } from '../../types'

const config: SiteConfig = {
  branding: { title: 'Test', titleEmphasis: '' },
  meta: { description: '', siteUrl: '' },
  nav: { sections: [] },
} as SiteConfig

describe('SiteConfigProvider', () => {
  it('exposes config via useSiteConfig', () => {
    const { result } = renderHook(() => useSiteConfig(), {
      wrapper: ({ children }) => <SiteConfigProvider config={config}>{children}</SiteConfigProvider>,
    })
    expect(result.current.branding.title).toBe('Test')
  })

  it('throws if used outside provider', () => {
    expect(() => renderHook(() => useSiteConfig())).toThrow(/SiteConfigProvider/)
  })
})
```

- [ ] **Step 2: Run to confirm failure**

Run: `npx vitest run packages/site-model/providers/__tests__/SiteConfigProvider.test.tsx`
Expected: FAIL

- [ ] **Step 3: Implement provider**

```tsx
import { createContext, useContext, type ReactNode } from 'react'
import type { SiteConfig } from '../types'

const SiteConfigContext = createContext<SiteConfig | null>(null)

export function SiteConfigProvider({ config, children }: { config: SiteConfig; children: ReactNode }) {
  return <SiteConfigContext.Provider value={config}>{children}</SiteConfigContext.Provider>
}

export function useSiteConfig(): SiteConfig {
  const ctx = useContext(SiteConfigContext)
  if (!ctx) throw new Error('useSiteConfig must be used inside <SiteConfigProvider>')
  return ctx
}
```

- [ ] **Step 4: Run to confirm pass**

Run: `npx vitest run packages/site-model/providers/__tests__/SiteConfigProvider.test.tsx`
Expected: PASS

- [ ] **Step 5: Export from index**

Append:

```ts
export { SiteConfigProvider, useSiteConfig } from './providers/SiteConfigProvider'
```

- [ ] **Step 6: Commit**

```bash
git add packages/site-model
git commit -m "feat(site-model): SiteConfigProvider + useSiteConfig"
```

### Task 1.9: ContentProvider + useContent

**Files:**
- Create: `packages/site-model/providers/ContentProvider.tsx`
- Create: `packages/site-model/providers/__tests__/ContentProvider.test.tsx`
- Modify: `packages/site-model/index.ts`

- [ ] **Step 1: Write failing test**

```tsx
import { describe, it, expect } from 'vitest'
import { renderHook } from '@testing-library/react'
import { ContentProvider, useContent } from '../ContentProvider'
import { SiteConfigProvider } from '../SiteConfigProvider'
import type { SiteConfig, SiteEntry } from '../../types'

const config: SiteConfig = {
  branding: { title: 't' },
  meta: { description: '', siteUrl: '' },
  hero: { heading: 'h', body: 'b' },
  nav: { sections: [{ key: 'g', label: 'Guides', path: '/g' }] },
}

const entries: SiteEntry[] = [
  {
    slug: '/g/a',
    section: 'g',
    raw: '',
    html: '',
    headings: [],
    frontmatter: { title: 'A' },
  },
]

describe('ContentProvider', () => {
  it('derives navTree from entries + config sections', () => {
    const { result } = renderHook(() => useContent(), {
      wrapper: ({ children }) => (
        <SiteConfigProvider config={config}>
          <ContentProvider entries={entries}>{children}</ContentProvider>
        </SiteConfigProvider>
      ),
    })
    expect(result.current.entries).toBe(entries)
    expect(result.current.navTree).toHaveLength(1)
    expect(result.current.navTree[0].children).toHaveLength(1)
    expect(result.current.navTree[0].children[0].label).toBe('A')
  })

  it('exposes findBySlug bound to current entries', () => {
    const { result } = renderHook(() => useContent(), {
      wrapper: ({ children }) => (
        <SiteConfigProvider config={config}>
          <ContentProvider entries={entries}>{children}</ContentProvider>
        </SiteConfigProvider>
      ),
    })
    expect(result.current.findBySlug('/g/a')?.frontmatter.title).toBe('A')
  })

  it('throws when used outside provider', () => {
    expect(() => renderHook(() => useContent())).toThrow(/ContentProvider/)
  })
})
```

- [ ] **Step 2: Run to confirm failure**

Run: `npx vitest run packages/site-model/providers/__tests__/ContentProvider.test.tsx`
Expected: FAIL

- [ ] **Step 3: Implement provider**

```tsx
import { createContext, useContext, useMemo, type ReactNode } from 'react'
import type { SiteEntry, NavNode } from '../types'
import { useSiteConfig } from './SiteConfigProvider'
import { buildNavTree } from '../lib/nav'
import { findBySlug, getBySection, getByDomain } from '../lib/lookup'
import { createSearchIndex, type SearchIndex } from '../lib/search'

export type ContentValue = {
  entries: SiteEntry[]
  navTree: NavNode[]
  searchIndex: SearchIndex
  findBySlug: (slug: string) => SiteEntry | undefined
  getBySection: (section: string) => SiteEntry[]
  getByDomain: (domain: string) => SiteEntry[]
}

const ContentContext = createContext<ContentValue | null>(null)

export function ContentProvider({ entries, children }: { entries: SiteEntry[]; children: ReactNode }) {
  const config = useSiteConfig()
  const value = useMemo<ContentValue>(() => {
    const navTree = buildNavTree(entries, config.nav.sections)
    const searchIndex = createSearchIndex(entries)
    return {
      entries,
      navTree,
      searchIndex,
      findBySlug: (slug) => findBySlug(entries, slug),
      getBySection: (section) => getBySection(entries, section),
      getByDomain: (domain) => getByDomain(entries, domain),
    }
  }, [entries, config.nav.sections])
  return <ContentContext.Provider value={value}>{children}</ContentContext.Provider>
}

export function useContent(): ContentValue {
  const ctx = useContext(ContentContext)
  if (!ctx) throw new Error('useContent must be used inside <ContentProvider>')
  return ctx
}
```

- [ ] **Step 4: Run to confirm pass**

Run: `npx vitest run packages/site-model/providers/__tests__/ContentProvider.test.tsx`
Expected: PASS

- [ ] **Step 5: Export from index**

Append:

```ts
export { ContentProvider, useContent, type ContentValue } from './providers/ContentProvider'
```

- [ ] **Step 6: Commit**

```bash
git add packages/site-model
git commit -m "feat(site-model): ContentProvider + useContent"
```

### Task 1.10: LinkProvider + useLink (router abstraction)

**Files:**
- Create: `packages/site-model/providers/LinkProvider.tsx`
- Create: `packages/site-model/providers/__tests__/LinkProvider.test.tsx`
- Modify: `packages/site-model/index.ts`

- [ ] **Step 1: Write failing test**

```tsx
import { describe, it, expect } from 'vitest'
import { render } from '@testing-library/react'
import { LinkProvider, useLink } from '../LinkProvider'
import type { ComponentType, AnchorHTMLAttributes } from 'react'

function Probe() {
  const Link = useLink()
  return <Link to="/x">click</Link>
}

describe('LinkProvider', () => {
  it('defaults to plain anchor with href', () => {
    const { container } = render(<Probe />)
    const anchor = container.querySelector('a')
    expect(anchor?.getAttribute('href')).toBe('/x')
    expect(anchor?.textContent).toBe('click')
  })

  it('honors injected Component', () => {
    const Custom: ComponentType<{ to: string; children?: any }> = ({ to, children }) => (
      <span data-router-link={to}>{children}</span>
    )
    const { container } = render(
      <LinkProvider Component={Custom}>
        <Probe />
      </LinkProvider>,
    )
    expect(container.querySelector('[data-router-link="/x"]')?.textContent).toBe('click')
  })
})
```

- [ ] **Step 2: Run to confirm failure**

Run: `npx vitest run packages/site-model/providers/__tests__/LinkProvider.test.tsx`
Expected: FAIL

- [ ] **Step 3: Implement provider**

```tsx
import { createContext, useContext, type ComponentType, type ReactNode, type AnchorHTMLAttributes } from 'react'

export type LinkComponentProps = AnchorHTMLAttributes<HTMLAnchorElement> & { to: string }
export type LinkComponent = ComponentType<LinkComponentProps>

const DefaultLink: LinkComponent = ({ to, children, ...rest }) => (
  <a href={to} {...rest}>{children}</a>
)

const LinkContext = createContext<LinkComponent>(DefaultLink)

export function LinkProvider({ Component, children }: { Component: LinkComponent; children: ReactNode }) {
  return <LinkContext.Provider value={Component}>{children}</LinkContext.Provider>
}

export function useLink(): LinkComponent {
  return useContext(LinkContext)
}
```

- [ ] **Step 4: Run to confirm pass**

Run: `npx vitest run packages/site-model/providers/__tests__/LinkProvider.test.tsx`
Expected: PASS

- [ ] **Step 5: Export from index**

Append:

```ts
export { LinkProvider, useLink, type LinkComponent, type LinkComponentProps } from './providers/LinkProvider'
```

- [ ] **Step 6: Commit**

```bash
git add packages/site-model
git commit -m "feat(site-model): LinkProvider + useLink"
```

### Task 1.11: RouteProvider + useCurrentRoute

**Files:**
- Create: `packages/site-model/providers/RouteProvider.tsx`
- Create: `packages/site-model/providers/__tests__/RouteProvider.test.tsx`
- Modify: `packages/site-model/index.ts`

- [ ] **Step 1: Write failing test**

```tsx
import { describe, it, expect, vi } from 'vitest'
import { renderHook, act } from '@testing-library/react'
import { RouteProvider, useCurrentRoute } from '../RouteProvider'

describe('RouteProvider', () => {
  it('exposes pathname/hash and a navigate fn', () => {
    const navigate = vi.fn()
    const { result } = renderHook(() => useCurrentRoute(), {
      wrapper: ({ children }) => (
        <RouteProvider pathname="/g/a" hash="#x" navigate={navigate}>
          {children}
        </RouteProvider>
      ),
    })
    expect(result.current.pathname).toBe('/g/a')
    expect(result.current.hash).toBe('#x')
    act(() => result.current.navigate('/elsewhere'))
    expect(navigate).toHaveBeenCalledWith('/elsewhere')
  })

  it('falls back to defaults when no provider', () => {
    const { result } = renderHook(() => useCurrentRoute())
    expect(result.current.pathname).toBe('/')
    expect(result.current.hash).toBe('')
    expect(typeof result.current.navigate).toBe('function')
  })
})
```

- [ ] **Step 2: Run to confirm failure**

Run: `npx vitest run packages/site-model/providers/__tests__/RouteProvider.test.tsx`
Expected: FAIL

- [ ] **Step 3: Implement provider**

```tsx
import { createContext, useContext, type ReactNode } from 'react'

export type RouteValue = {
  pathname: string
  hash: string
  navigate: (to: string) => void
}

const DEFAULT_ROUTE: RouteValue = { pathname: '/', hash: '', navigate: () => {} }
const RouteContext = createContext<RouteValue>(DEFAULT_ROUTE)

export function RouteProvider({
  pathname,
  hash,
  navigate,
  children,
}: RouteValue & { children: ReactNode }) {
  return <RouteContext.Provider value={{ pathname, hash, navigate }}>{children}</RouteContext.Provider>
}

export function useCurrentRoute(): RouteValue {
  return useContext(RouteContext)
}
```

- [ ] **Step 4: Run to confirm pass**

Run: `npx vitest run packages/site-model/providers/__tests__/RouteProvider.test.tsx`
Expected: PASS

- [ ] **Step 5: Export from index**

Append:

```ts
export { RouteProvider, useCurrentRoute, type RouteValue } from './providers/RouteProvider'
```

- [ ] **Step 6: Commit**

```bash
git add packages/site-model
git commit -m "feat(site-model): RouteProvider + useCurrentRoute"
```

### Task 1.12: useSearchState hook (extracted state machine)

**Files:**
- Create: `packages/site-model/hooks/useSearchState.ts`
- Create: `packages/site-model/hooks/__tests__/useSearchState.test.ts`
- Modify: `packages/site-model/index.ts`

- [ ] **Step 1: Write failing test**

```ts
import { describe, it, expect } from 'vitest'
import { renderHook, act } from '@testing-library/react'
import { useSearchState } from '../useSearchState'
import { createSearchIndex } from '../../lib/search'
import type { SiteEntry } from '../../types'

const make = (slug: string, title: string): SiteEntry => ({
  slug,
  section: 's',
  raw: '',
  html: '',
  headings: [],
  frontmatter: { title },
})

const entries: SiteEntry[] = [
  make('/a', 'Alpha'),
  make('/b', 'Alphabet'),
  make('/c', 'Beta'),
]
const idx = createSearchIndex(entries)

describe('useSearchState', () => {
  it('starts empty', () => {
    const { result } = renderHook(() => useSearchState(idx))
    expect(result.current.query).toBe('')
    expect(result.current.results).toEqual([])
    expect(result.current.selectedIndex).toBe(0)
  })

  it('updates results when query changes', () => {
    const { result } = renderHook(() => useSearchState(idx))
    act(() => result.current.setQuery('alph'))
    expect(result.current.results.length).toBeGreaterThan(0)
  })

  it('arrow keys move selection within bounds', () => {
    const { result } = renderHook(() => useSearchState(idx))
    act(() => result.current.setQuery('alph'))
    const total = result.current.results.length
    act(() => result.current.handleKey({ key: 'ArrowDown' } as any))
    expect(result.current.selectedIndex).toBe(Math.min(1, total - 1))
    act(() => result.current.handleKey({ key: 'ArrowUp' } as any))
    expect(result.current.selectedIndex).toBe(0)
  })

  it('Escape resets query and clears results', () => {
    const { result } = renderHook(() => useSearchState(idx))
    act(() => result.current.setQuery('alph'))
    act(() => result.current.handleKey({ key: 'Escape' } as any))
    expect(result.current.query).toBe('')
    expect(result.current.results).toEqual([])
  })
})
```

- [ ] **Step 2: Run to confirm failure**

Run: `npx vitest run packages/site-model/hooks/__tests__/useSearchState.test.ts`
Expected: FAIL

- [ ] **Step 3: Implement hook**

```ts
import { useMemo, useState, useCallback, type KeyboardEvent } from 'react'
import type { SearchIndex, SearchResult } from '../lib/search'

export type SearchState = {
  query: string
  setQuery: (q: string) => void
  results: SearchResult[]
  selectedIndex: number
  setSelectedIndex: (i: number) => void
  handleKey: (e: KeyboardEvent<HTMLElement>) => void
  reset: () => void
}

export function useSearchState(index: SearchIndex): SearchState {
  const [query, setQueryState] = useState('')
  const [selectedIndex, setSelectedIndex] = useState(0)

  const results = useMemo(() => index.query(query), [index, query])

  const setQuery = useCallback((q: string) => {
    setQueryState(q)
    setSelectedIndex(0)
  }, [])

  const reset = useCallback(() => {
    setQueryState('')
    setSelectedIndex(0)
  }, [])

  const handleKey = useCallback(
    (e: KeyboardEvent<HTMLElement>) => {
      if (e.key === 'ArrowDown') {
        setSelectedIndex((i) => Math.min(i + 1, Math.max(results.length - 1, 0)))
      } else if (e.key === 'ArrowUp') {
        setSelectedIndex((i) => Math.max(i - 1, 0))
      } else if (e.key === 'Escape') {
        reset()
      }
    },
    [results.length, reset],
  )

  return { query, setQuery, results, selectedIndex, setSelectedIndex, handleKey, reset }
}
```

- [ ] **Step 4: Run to confirm pass**

Run: `npx vitest run packages/site-model/hooks/__tests__/useSearchState.test.ts`
Expected: PASS (4 tests)

- [ ] **Step 5: Export from index**

Append:

```ts
export { useSearchState, type SearchState } from './hooks/useSearchState'
```

- [ ] **Step 6: Commit**

```bash
git add packages/site-model
git commit -m "feat(site-model): useSearchState hook with tests"
```

### Task 1.13: Run full test suite to confirm site-model is green

- [ ] **Step 1: Run full vitest**

Run: `npm test`
Expected: PASS (existing 102 tests + new tests for site-model — total approximately 120+ tests)

- [ ] **Step 2: TypeScript check across project**

Run: `npx tsc --noEmit`
Expected: PASS

If any errors surface, fix them. Likely candidates: missing exports in `index.ts`, type drift between local types and `reference-web-site` types.

- [ ] **Step 3: No commit** — this is a verification task.

### Task 1.14: Site-model demo example in `site/`

**Files:**
- Create: `site/examples/site-model/index.tsx`
- Modify: `site/src/manifest.ts`

- [ ] **Step 1: Create the example**

```tsx
import { useState } from 'react'
import {
  SiteConfigProvider,
  ContentProvider,
  useContent,
  useSiteConfig,
  type SiteConfig,
  type SiteEntry,
} from '@agentic-cookbook/agentic-web-toolkit/site-model'

export const meta = { id: 'site-model', label: 'Site Model' }

const config: SiteConfig = {
  branding: { title: 'Site Model Demo', titleEmphasis: 'The' },
  meta: { description: '', siteUrl: '' },
  hero: { heading: 'Site Model', body: '' },
  nav: {
    sections: [
      { key: 'guides', label: 'Guides', path: '/guides' },
      { key: 'api', label: 'API', path: '/api' },
    ],
  },
}

const make = (
  slug: string,
  section: string,
  title: string,
  summary: string,
): SiteEntry => ({
  slug,
  section,
  raw: '',
  html: '',
  headings: [],
  frontmatter: { title, summary },
})

const entries: SiteEntry[] = [
  make('/guides/install', 'guides', 'Install', 'Install the toolkit'),
  make('/guides/configure', 'guides', 'Configure', 'Set up your config'),
  make('/api/auth', 'api', 'Authentication', 'API auth flows'),
]

function Inner() {
  const { branding } = useSiteConfig()
  const { entries: e, navTree, searchIndex } = useContent()
  const [q, setQ] = useState('')
  const results = searchIndex.query(q)
  return (
    <div className="awt-example-content" style={{ padding: 24, color: 'var(--color-text-primary)', background: 'var(--color-surface)', minHeight: '100vh' }}>
      <h1 style={{ color: 'var(--color-accent)' }}>{branding.title}</h1>
      <p>Loaded {e.length} entries across {navTree.length} sections.</p>
      <h2>Nav tree</h2>
      <ul>
        {navTree.map((n) => (
          <li key={n.path}>
            <strong>{n.label}</strong>
            <ul>
              {n.children.map((c) => <li key={c.path}>{c.label}</li>)}
            </ul>
          </li>
        ))}
      </ul>
      <h2>Search</h2>
      <input
        placeholder="Try 'install' or 'auth'"
        value={q}
        onChange={(e) => setQ(e.target.value)}
        style={{ padding: 8, width: '100%', maxWidth: 400, background: 'var(--color-surface-raised)', color: 'var(--color-text-primary)', border: '1px solid var(--color-border)' }}
      />
      <ul>
        {results.map((r) => (
          <li key={r.entry.slug}>
            {r.entry.frontmatter.title}{' '}
            <span style={{ opacity: 0.6 }}>— {String(r.entry.frontmatter.summary ?? '')}</span>
          </li>
        ))}
      </ul>
    </div>
  )
}

export default function SiteModelExample() {
  return (
    <SiteConfigProvider config={config}>
      <ContentProvider entries={entries}>
        <Inner />
      </ContentProvider>
    </SiteConfigProvider>
  )
}
```

- [ ] **Step 2: Register in manifest**

Edit `site/src/manifest.ts`. Add the import and registry entry:

```ts
import type { ComponentType } from 'react'
import * as Chat from '../examples/chat'
import * as ReferenceWebSite from './examples/reference-web-site'
import * as SiteModel from '../examples/site-model'

export type ExampleEntry = {
  id: string
  label: string
  Component: ComponentType
}

export const examples: ExampleEntry[] = [
  { ...Chat.meta, Component: Chat.default },
  { ...SiteModel.meta, Component: SiteModel.default },
  { ...ReferenceWebSite.meta, Component: ReferenceWebSite.default },
]
```

- [ ] **Step 3: Run the dev server and visually verify**

Run: `cd site && npm run dev` (background)

Open http://localhost:5173 (or whichever port Vite reports). Click "Site Model" in the rail. Confirm:
- Heading shows "Site Model Demo" in accent color
- Nav tree lists Guides (Install, Configure) and API (Authentication)
- Typing "install" in the search box surfaces the Install entry
- Switching themes via the rail's theme picker re-skins the panel

Stop the dev server.

- [ ] **Step 4: TypeScript build check**

Run: `cd site && npx tsc --noEmit`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add site/examples/site-model site/src/manifest.ts
git commit -m "feat(site): add site-model demo example"
```

### Task 1.15: Phase 1 wrap-up

- [ ] **Step 1: Full repo test + typecheck**

Run: `npm test && npx tsc --noEmit`
Expected: all green

- [ ] **Step 2: Push**

```bash
git push
```

---

## Phase 2: `packages/layout/`

### Task 2.1: Add `layout` exports to root package.json

**Files:**
- Modify: `package.json` (root)

- [ ] **Step 1: Edit `exports`**

Add (alongside other package entries):

```json
"./layout": "./packages/layout/index.ts",
"./layout/css/base.css": "./packages/layout/css/base.css",
"./layout/css/header.css": "./packages/layout/css/header.css",
"./layout/css/sidebar.css": "./packages/layout/css/sidebar.css",
"./layout/css/breadcrumbs.css": "./packages/layout/css/breadcrumbs.css",
"./layout/css/toc.css": "./packages/layout/css/toc.css",
"./layout/css/app-shell.css": "./packages/layout/css/app-shell.css",
```

- [ ] **Step 2: Commit**

```bash
git add package.json
git commit -m "chore: add layout package export entries"
```

### Task 2.2: Create `layout` skeleton + base.css

**Files:**
- Create: `packages/layout/package.json`
- Create: `packages/layout/index.ts`
- Create: `packages/layout/css/base.css`
- Create: `packages/layout/README.md`

- [ ] **Step 1: package.json**

```json
{
  "name": "@agentic-cookbook/layout",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./index.ts",
  "types": "./index.ts",
  "peerDependencies": {
    "react": "^19.0.0"
  }
}
```

- [ ] **Step 2: Placeholder index**

```ts
// packages/layout/index.ts
export {} // populated by subsequent tasks
```

- [ ] **Step 3: base.css with shared CSS variables (no literal colors)**

```css
/* packages/layout/css/base.css
 * Shared structural styles for Agentic layout components. Import once.
 * All visual properties resolve through theme tokens (--color-*, --font-*).
 */

.awt-layout {
  --awt-gutter: 16px;
  --awt-radius-sm: 4px;
  --awt-radius-md: 6px;
  --awt-transition: 150ms ease;
  font-family: var(--font-sans, system-ui, sans-serif);
  color: var(--color-text-primary);
  background: var(--color-surface);
}

.awt-layout *,
.awt-layout *::before,
.awt-layout *::after {
  box-sizing: border-box;
}
```

- [ ] **Step 4: README**

```markdown
# @agentic-cookbook/layout

Structural layout primitives for Agentic Cookbook sites. Components are
prop-driven; connected variants read from `@agentic-cookbook/site-model`.
All CSS uses theme tokens from `@agentic-cookbook/agentic-web-toolkit/themes`.
```

- [ ] **Step 5: Commit**

```bash
git add packages/layout
git commit -m "feat(layout): create package skeleton + base.css"
```

### Task 2.3: Breadcrumbs (prop-driven + connected)

**Files:**
- Create: `packages/layout/css/breadcrumbs.css`
- Create: `packages/layout/components/Breadcrumbs.tsx`
- Create: `packages/layout/components/BreadcrumbsConnected.tsx`
- Create: `packages/layout/components/__tests__/Breadcrumbs.test.tsx`
- Modify: `packages/layout/index.ts`

- [ ] **Step 1: Write failing test**

```tsx
import { describe, it, expect } from 'vitest'
import { render } from '@testing-library/react'
import { Breadcrumbs } from '../Breadcrumbs'

describe('Breadcrumbs', () => {
  it('renders one item per breadcrumb entry', () => {
    const { container } = render(
      <Breadcrumbs trail={[{ label: 'Home', path: '/' }, { label: 'Guides', path: '/g' }]} />,
    )
    const items = container.querySelectorAll('.awt-breadcrumbs__item')
    expect(items).toHaveLength(2)
    expect(items[0].textContent).toContain('Home')
    expect(items[1].textContent).toContain('Guides')
  })

  it('marks the last item as current', () => {
    const { container } = render(
      <Breadcrumbs trail={[{ label: 'Home', path: '/' }, { label: 'Guides', path: '/g' }]} />,
    )
    const last = container.querySelector('.awt-breadcrumbs__item--current')
    expect(last?.textContent).toContain('Guides')
  })
})
```

- [ ] **Step 2: Run test to fail**

Run: `npx vitest run packages/layout/components/__tests__/Breadcrumbs.test.tsx`
Expected: FAIL

- [ ] **Step 3: CSS**

```css
/* packages/layout/css/breadcrumbs.css */
.awt-breadcrumbs {
  display: flex;
  flex-wrap: wrap;
  align-items: center;
  gap: 0.4rem;
  font-size: 0.85rem;
  color: var(--color-text-secondary);
  font-family: var(--font-sans, inherit);
}
.awt-breadcrumbs__item {
  display: inline-flex;
  align-items: center;
  gap: 0.4rem;
}
.awt-breadcrumbs__item a {
  color: var(--color-text-secondary);
  text-decoration: none;
  transition: color var(--awt-transition, 150ms ease);
}
.awt-breadcrumbs__item a:hover { color: var(--color-accent); }
.awt-breadcrumbs__item--current { color: var(--color-text-primary); }
.awt-breadcrumbs__separator {
  color: var(--color-text-dim);
  user-select: none;
}
```

- [ ] **Step 4: Component**

```tsx
// packages/layout/components/Breadcrumbs.tsx
import { useLink, type BreadcrumbEntry } from '@agentic-cookbook/agentic-web-toolkit/site-model'

export type BreadcrumbsProps = {
  trail: BreadcrumbEntry[]
  separator?: string
}

export function Breadcrumbs({ trail, separator = '/' }: BreadcrumbsProps) {
  const Link = useLink()
  return (
    <nav className="awt-breadcrumbs" aria-label="Breadcrumb">
      {trail.map((entry, i) => {
        const isLast = i === trail.length - 1
        return (
          <span
            key={entry.path}
            className={`awt-breadcrumbs__item${isLast ? ' awt-breadcrumbs__item--current' : ''}`}
          >
            {isLast ? <span>{entry.label}</span> : <Link to={entry.path}>{entry.label}</Link>}
            {!isLast && <span className="awt-breadcrumbs__separator">{separator}</span>}
          </span>
        )
      })}
    </nav>
  )
}
```

Note: The `BreadcrumbEntry` type comes from `site-model`. Re-export from there is required — confirm `packages/site-model/index.ts` exports `type BreadcrumbEntry`.

- [ ] **Step 5: Connected variant**

```tsx
// packages/layout/components/BreadcrumbsConnected.tsx
import { useCurrentRoute, slugToBreadcrumbs } from '@agentic-cookbook/agentic-web-toolkit/site-model'
import { Breadcrumbs, type BreadcrumbsProps } from './Breadcrumbs'

export function BreadcrumbsConnected(props: Omit<BreadcrumbsProps, 'trail'>) {
  const { pathname } = useCurrentRoute()
  return <Breadcrumbs trail={slugToBreadcrumbs(pathname)} {...props} />
}
```

- [ ] **Step 6: Export from `packages/layout/index.ts`**

Replace the placeholder content:

```ts
export { Breadcrumbs, type BreadcrumbsProps } from './components/Breadcrumbs'
export { BreadcrumbsConnected } from './components/BreadcrumbsConnected'
```

- [ ] **Step 7: Run test to pass**

Run: `npx vitest run packages/layout/components/__tests__/Breadcrumbs.test.tsx`
Expected: PASS

- [ ] **Step 8: Commit**

```bash
git add packages/layout package.json
git commit -m "feat(layout): Breadcrumbs (prop-driven + connected)"
```

### Task 2.4: TableOfContents (prop-driven + connected)

**Files:**
- Create: `packages/layout/css/toc.css`
- Create: `packages/layout/components/TableOfContents.tsx`
- Create: `packages/layout/components/TableOfContentsConnected.tsx`
- Create: `packages/layout/components/__tests__/TableOfContents.test.tsx`
- Modify: `packages/layout/index.ts`

- [ ] **Step 1: Failing test**

```tsx
import { describe, it, expect } from 'vitest'
import { render } from '@testing-library/react'
import { TableOfContents } from '../TableOfContents'

describe('TableOfContents', () => {
  it('renders one item per heading', () => {
    const { container } = render(
      <TableOfContents
        headings={[
          { id: 'a', text: 'A', depth: 2 },
          { id: 'b', text: 'B', depth: 3 },
        ]}
      />,
    )
    expect(container.querySelectorAll('.awt-toc__item')).toHaveLength(2)
  })

  it('applies depth-based class for indentation', () => {
    const { container } = render(
      <TableOfContents headings={[{ id: 'a', text: 'A', depth: 3 }]} />,
    )
    expect(container.querySelector('.awt-toc__item--depth-3')).not.toBeNull()
  })

  it('renders nothing when no headings', () => {
    const { container } = render(<TableOfContents headings={[]} />)
    expect(container.querySelector('.awt-toc')).toBeNull()
  })

  it('respects minDepth filter', () => {
    const { container } = render(
      <TableOfContents
        minDepth={3}
        headings={[
          { id: 'a', text: 'A', depth: 2 },
          { id: 'b', text: 'B', depth: 3 },
        ]}
      />,
    )
    expect(container.querySelectorAll('.awt-toc__item')).toHaveLength(1)
  })
})
```

- [ ] **Step 2: Run to fail**

Run: `npx vitest run packages/layout/components/__tests__/TableOfContents.test.tsx`
Expected: FAIL

- [ ] **Step 3: CSS**

```css
/* packages/layout/css/toc.css */
.awt-toc {
  font-size: 0.82rem;
  font-family: var(--font-sans, inherit);
  color: var(--color-text-secondary);
}
.awt-toc__title {
  text-transform: uppercase;
  letter-spacing: 0.08em;
  font-size: 0.7rem;
  margin-bottom: 0.6rem;
  color: var(--color-text-dim);
}
.awt-toc__list { list-style: none; margin: 0; padding: 0; }
.awt-toc__item { padding: 0.2rem 0; }
.awt-toc__item--depth-3 { padding-left: 0.9rem; }
.awt-toc__item--depth-4 { padding-left: 1.8rem; }
.awt-toc__item a {
  color: var(--color-text-secondary);
  text-decoration: none;
  display: block;
  border-left: 2px solid transparent;
  padding-left: 0.6rem;
  transition: color var(--awt-transition, 150ms ease), border-color var(--awt-transition, 150ms ease);
}
.awt-toc__item a:hover { color: var(--color-text-primary); }
.awt-toc__item--active a {
  color: var(--color-accent);
  border-left-color: var(--color-accent);
}
```

- [ ] **Step 4: Component**

Read `packages/reference-web-site/components/layout/TableOfContents.tsx` for the IntersectionObserver logic; adapt to a fully prop-driven implementation. Use `HeadingEntry.depth` (the canonical field name) and a `minDepth` prop instead of a per-heading `hidden` flag (HeadingEntry has no `hidden`).

```tsx
// packages/layout/components/TableOfContents.tsx
import { useEffect, useState } from 'react'
import type { HeadingEntry } from '@agentic-cookbook/agentic-web-toolkit/site-model'

export type TableOfContentsProps = {
  headings: HeadingEntry[]
  title?: string
  minDepth?: number
  maxDepth?: number
}

export function TableOfContents({
  headings,
  title = 'On this page',
  minDepth = 2,
  maxDepth = 4,
}: TableOfContentsProps) {
  const visible = headings.filter((h) => h.depth >= minDepth && h.depth <= maxDepth)
  const [activeId, setActiveId] = useState<string | null>(visible[0]?.id ?? null)

  useEffect(() => {
    if (visible.length === 0 || typeof IntersectionObserver === 'undefined') return
    const observer = new IntersectionObserver(
      (entries) => {
        for (const entry of entries) {
          if (entry.isIntersecting) {
            setActiveId(entry.target.id)
            break
          }
        }
      },
      { rootMargin: '-20% 0px -70% 0px', threshold: 0 },
    )
    for (const h of visible) {
      const el = document.getElementById(h.id)
      if (el) observer.observe(el)
    }
    return () => observer.disconnect()
  }, [visible])

  if (visible.length === 0) return null

  return (
    <nav className="awt-toc" aria-label="Table of contents">
      <div className="awt-toc__title">{title}</div>
      <ul className="awt-toc__list">
        {visible.map((h) => (
          <li
            key={h.id}
            className={`awt-toc__item awt-toc__item--depth-${h.depth}${
              h.id === activeId ? ' awt-toc__item--active' : ''
            }`}
          >
            <a href={`#${h.id}`}>{h.text}</a>
          </li>
        ))}
      </ul>
    </nav>
  )
}
```

- [ ] **Step 5: Connected variant**

```tsx
// packages/layout/components/TableOfContentsConnected.tsx
import { useContent, useCurrentRoute } from '@agentic-cookbook/agentic-web-toolkit/site-model'
import { TableOfContents, type TableOfContentsProps } from './TableOfContents'

export function TableOfContentsConnected(props: Omit<TableOfContentsProps, 'headings'>) {
  const { findBySlug } = useContent()
  const { pathname } = useCurrentRoute()
  const entry = findBySlug(pathname)
  return <TableOfContents headings={entry?.headings ?? []} {...props} />
}
```

- [ ] **Step 6: Export**

Append to `packages/layout/index.ts`:

```ts
export { TableOfContents, type TableOfContentsProps } from './components/TableOfContents'
export { TableOfContentsConnected } from './components/TableOfContentsConnected'
```

- [ ] **Step 7: Run to pass**

Run: `npx vitest run packages/layout/components/__tests__/TableOfContents.test.tsx`
Expected: PASS

- [ ] **Step 8: Commit**

```bash
git add packages/layout
git commit -m "feat(layout): TableOfContents (prop-driven + connected)"
```

### Task 2.5: Header (prop-driven + connected)

**Files:**
- Create: `packages/layout/css/header.css`
- Create: `packages/layout/components/Header.tsx`
- Create: `packages/layout/components/HeaderConnected.tsx`
- Create: `packages/layout/components/__tests__/Header.test.tsx`
- Modify: `packages/layout/index.ts`

- [ ] **Step 1: Failing test**

```tsx
import { describe, it, expect, vi } from 'vitest'
import { render, fireEvent } from '@testing-library/react'
import { Header } from '../Header'

describe('Header', () => {
  it('renders title and emphasis', () => {
    const { container } = render(<Header title="Site" titleEmphasis="The" />)
    expect(container.textContent).toContain('The')
    expect(container.textContent).toContain('Site')
  })

  it('fires onSearchOpen when search button clicked', () => {
    const onSearchOpen = vi.fn()
    const { getByRole } = render(<Header title="x" onSearchOpen={onSearchOpen} />)
    fireEvent.click(getByRole('button', { name: /search/i }))
    expect(onSearchOpen).toHaveBeenCalled()
  })

  it('omits search button when onSearchOpen not provided', () => {
    const { queryByRole } = render(<Header title="x" />)
    expect(queryByRole('button', { name: /search/i })).toBeNull()
  })
})
```

- [ ] **Step 2: Run to fail**

Run: `npx vitest run packages/layout/components/__tests__/Header.test.tsx`
Expected: FAIL

- [ ] **Step 3: CSS**

```css
/* packages/layout/css/header.css */
.awt-header {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  padding: 0.75rem 1rem;
  border-bottom: 1px solid var(--color-border);
  background: var(--color-surface-raised);
  color: var(--color-text-primary);
  font-family: var(--font-sans, inherit);
}
.awt-header__brand {
  display: inline-flex;
  align-items: center;
  gap: 0.5rem;
  font-weight: 600;
  font-size: 1rem;
  text-decoration: none;
  color: inherit;
}
.awt-header__brand-emphasis {
  color: var(--color-accent);
  font-style: italic;
  font-weight: 400;
}
.awt-header__spacer { flex: 1; }
.awt-header__action {
  background: transparent;
  border: 1px solid var(--color-border);
  border-radius: var(--awt-radius-sm, 4px);
  color: var(--color-text-secondary);
  padding: 0.35rem 0.6rem;
  font: inherit;
  cursor: pointer;
  transition: color var(--awt-transition, 150ms ease), border-color var(--awt-transition, 150ms ease);
}
.awt-header__action:hover {
  color: var(--color-accent);
  border-color: var(--color-accent);
}
.awt-header__action--icon { padding: 0.35rem 0.5rem; }
```

- [ ] **Step 4: Component**

```tsx
// packages/layout/components/Header.tsx
import { useLink } from '@agentic-cookbook/agentic-web-toolkit/site-model'
import type { ReactNode } from 'react'

export type HeaderProps = {
  title: string
  titleEmphasis?: string
  homeHref?: string
  onMenuToggle?: () => void
  onSearchOpen?: () => void
  rightSlot?: ReactNode
}

export function Header({
  title,
  titleEmphasis,
  homeHref = '/',
  onMenuToggle,
  onSearchOpen,
  rightSlot,
}: HeaderProps) {
  const Link = useLink()
  return (
    <header className="awt-header" role="banner">
      {onMenuToggle && (
        <button
          type="button"
          className="awt-header__action awt-header__action--icon"
          aria-label="Toggle menu"
          onClick={onMenuToggle}
        >
          ☰
        </button>
      )}
      <Link to={homeHref} className="awt-header__brand">
        {titleEmphasis && <span className="awt-header__brand-emphasis">{titleEmphasis}</span>}
        <span>{title}</span>
      </Link>
      <div className="awt-header__spacer" />
      {onSearchOpen && (
        <button
          type="button"
          className="awt-header__action"
          aria-label="Open search"
          onClick={onSearchOpen}
        >
          Search
        </button>
      )}
      {rightSlot}
    </header>
  )
}
```

- [ ] **Step 5: Connected variant**

```tsx
// packages/layout/components/HeaderConnected.tsx
import { useSiteConfig } from '@agentic-cookbook/agentic-web-toolkit/site-model'
import { Header, type HeaderProps } from './Header'

export type HeaderConnectedProps = Omit<HeaderProps, 'title' | 'titleEmphasis'>

export function HeaderConnected(props: HeaderConnectedProps) {
  const { branding } = useSiteConfig()
  return (
    <Header
      title={branding.title}
      titleEmphasis={branding.titleEmphasis}
      {...props}
    />
  )
}
```

- [ ] **Step 6: Export**

Append:

```ts
export { Header, type HeaderProps } from './components/Header'
export { HeaderConnected, type HeaderConnectedProps } from './components/HeaderConnected'
```

- [ ] **Step 7: Run to pass**

Run: `npx vitest run packages/layout/components/__tests__/Header.test.tsx`
Expected: PASS

- [ ] **Step 8: Commit**

```bash
git add packages/layout
git commit -m "feat(layout): Header (prop-driven + connected)"
```

### Task 2.6: Sidebar (prop-driven + connected) — the marquee task

**Files:**
- Create: `packages/layout/css/sidebar.css`
- Create: `packages/layout/components/Sidebar.tsx`
- Create: `packages/layout/components/SidebarConnected.tsx`
- Create: `packages/layout/components/__tests__/Sidebar.test.tsx`
- Modify: `packages/layout/index.ts`

- [ ] **Step 1: Failing test**

```tsx
import { describe, it, expect, vi } from 'vitest'
import { render, fireEvent } from '@testing-library/react'
import { Sidebar } from '../Sidebar'
import type { NavNode } from '@agentic-cookbook/agentic-web-toolkit/site-model'

const tree: NavNode[] = [
  {
    label: 'Guides',
    path: '/guides',
    children: [
      { label: 'Install', path: '/guides/install', children: [] },
      { label: 'Configure', path: '/guides/configure', children: [] },
    ],
  },
]

describe('Sidebar', () => {
  it('renders one section per top-level node', () => {
    const { container } = render(<Sidebar nodes={tree} currentPath="/" />)
    expect(container.querySelectorAll('.awt-sidebar__section')).toHaveLength(1)
  })

  it('renders one item per leaf', () => {
    const { container } = render(<Sidebar nodes={tree} currentPath="/" />)
    expect(container.querySelectorAll('.awt-sidebar__item')).toHaveLength(2)
  })

  it('marks the current path as active', () => {
    const { container } = render(<Sidebar nodes={tree} currentPath="/guides/install" />)
    const active = container.querySelector('.awt-sidebar__item--active')
    expect(active?.textContent).toContain('Install')
  })

  it('fires onNavigate when an item is clicked', () => {
    const onNavigate = vi.fn()
    const { getByText } = render(<Sidebar nodes={tree} currentPath="/" onNavigate={onNavigate} />)
    fireEvent.click(getByText('Configure'))
    expect(onNavigate).toHaveBeenCalledWith('/guides/configure')
  })

  it('renders nested children recursively', () => {
    const nested: NavNode[] = [
      {
        label: 'Top',
        path: '/top',
        children: [
          {
            label: 'Group',
            path: '/top/group',
            children: [
              { label: 'Leaf', path: '/top/group/leaf', children: [] },
            ],
          },
        ],
      },
    ]
    const { getByText } = render(<Sidebar nodes={nested} currentPath="/" />)
    expect(getByText('Leaf')).toBeTruthy()
    expect(getByText('Group')).toBeTruthy()
  })
})
```

- [ ] **Step 2: Run to fail**

Run: `npx vitest run packages/layout/components/__tests__/Sidebar.test.tsx`
Expected: FAIL

- [ ] **Step 3: CSS**

```css
/* packages/layout/css/sidebar.css */
.awt-sidebar {
  display: flex;
  flex-direction: column;
  gap: 1rem;
  padding: 1rem 0.75rem;
  background: var(--color-surface-raised);
  border-right: 1px solid var(--color-border);
  font-family: var(--font-sans, inherit);
  font-size: 0.88rem;
  color: var(--color-text-primary);
  height: 100%;
  overflow-y: auto;
}
.awt-sidebar__section { display: flex; flex-direction: column; gap: 0.25rem; }
.awt-sidebar__section-header {
  font-size: 0.7rem;
  font-weight: 600;
  text-transform: uppercase;
  letter-spacing: 0.08em;
  color: var(--color-text-dim);
  padding: 0 0.5rem;
  margin: 0 0 0.25rem;
}
.awt-sidebar__list { list-style: none; padding: 0; margin: 0; display: flex; flex-direction: column; gap: 1px; }
.awt-sidebar__list--nested { padding-left: 0.6rem; border-left: 1px solid var(--color-border-subtle, var(--color-border)); margin-left: 0.5rem; }
.awt-sidebar__group { display: flex; flex-direction: column; gap: 1px; padding: 0.2rem 0; }
.awt-sidebar__group-label { font-size: 0.75rem; font-weight: 600; color: var(--color-text-secondary); padding: 0.25rem 0.6rem; }
.awt-sidebar__item a, .awt-sidebar__item button {
  display: block;
  width: 100%;
  text-align: left;
  background: transparent;
  border: 1px solid transparent;
  border-radius: var(--awt-radius-sm, 4px);
  padding: 0.4rem 0.6rem;
  color: var(--color-text-secondary);
  font: inherit;
  text-decoration: none;
  cursor: pointer;
  transition: background var(--awt-transition, 150ms ease), color var(--awt-transition, 150ms ease);
}
.awt-sidebar__item a:hover, .awt-sidebar__item button:hover {
  background: var(--color-surface-hover, rgba(0,0,0,0.04));
  color: var(--color-text-primary);
}
.awt-sidebar__item--active a, .awt-sidebar__item--active button {
  background: var(--color-accent-dim);
  color: var(--color-accent);
}
```

- [ ] **Step 4: Component**

The Sidebar handles arbitrarily nested `NavNode` trees. Section nodes (top-level) get a heading; deeper containers (nodes with children) render as nested groups. Leaves are clickable items.

```tsx
// packages/layout/components/Sidebar.tsx
import { useLink, type NavNode, type LinkComponent } from '@agentic-cookbook/agentic-web-toolkit/site-model'
import type { ReactNode } from 'react'

export type SidebarProps = {
  nodes: NavNode[]
  currentPath: string
  onNavigate?: (path: string) => void
  emptyState?: ReactNode
}

function renderItem(
  node: NavNode,
  currentPath: string,
  onNavigate: ((p: string) => void) | undefined,
  Link: LinkComponent,
): ReactNode {
  const isActive = node.path === currentPath
  if (node.children.length === 0) {
    return (
      <li
        key={node.path}
        className={`awt-sidebar__item${isActive ? ' awt-sidebar__item--active' : ''}`}
      >
        {onNavigate ? (
          <button type="button" onClick={() => onNavigate(node.path)}>
            {node.label}
          </button>
        ) : (
          <Link to={node.path}>{node.label}</Link>
        )}
      </li>
    )
  }
  return (
    <li key={node.path} className="awt-sidebar__group">
      <div className="awt-sidebar__group-label">{node.label}</div>
      <ul className="awt-sidebar__list awt-sidebar__list--nested">
        {node.children.map((c) => renderItem(c, currentPath, onNavigate, Link))}
      </ul>
    </li>
  )
}

export function Sidebar({ nodes, currentPath, onNavigate, emptyState }: SidebarProps) {
  const Link = useLink()
  if (nodes.length === 0) {
    return <aside className="awt-sidebar">{emptyState ?? null}</aside>
  }
  return (
    <aside className="awt-sidebar" aria-label="Site navigation">
      {nodes.map((section) => (
        <div key={section.path} className="awt-sidebar__section">
          <h2 className="awt-sidebar__section-header">{section.label}</h2>
          <ul className="awt-sidebar__list">
            {section.children.map((c) => renderItem(c, currentPath, onNavigate, Link))}
          </ul>
        </div>
      ))}
    </aside>
  )
}
```

- [ ] **Step 5: Connected variant**

```tsx
// packages/layout/components/SidebarConnected.tsx
import { useContent, useCurrentRoute } from '@agentic-cookbook/agentic-web-toolkit/site-model'
import { Sidebar, type SidebarProps } from './Sidebar'

export function SidebarConnected(props: Omit<SidebarProps, 'nodes' | 'currentPath'>) {
  const { navTree } = useContent()
  const { pathname } = useCurrentRoute()
  return <Sidebar nodes={navTree} currentPath={pathname} {...props} />
}
```

- [ ] **Step 6: Export**

Append:

```ts
export { Sidebar, type SidebarProps } from './components/Sidebar'
export { SidebarConnected } from './components/SidebarConnected'
```

- [ ] **Step 7: Run to pass**

Run: `npx vitest run packages/layout/components/__tests__/Sidebar.test.tsx`
Expected: PASS

- [ ] **Step 8: Commit**

```bash
git add packages/layout
git commit -m "feat(layout): Sidebar (prop-driven + connected)"
```

### Task 2.7: AppShell

**Files:**
- Create: `packages/layout/css/app-shell.css`
- Create: `packages/layout/components/AppShell.tsx`
- Create: `packages/layout/components/__tests__/AppShell.test.tsx`
- Modify: `packages/layout/index.ts`

- [ ] **Step 1: Failing test**

```tsx
import { describe, it, expect } from 'vitest'
import { render } from '@testing-library/react'
import { AppShell } from '../AppShell'

describe('AppShell', () => {
  it('renders header, sidebar, main, toc slots', () => {
    const { container } = render(
      <AppShell
        header={<div data-test="h">H</div>}
        sidebar={<div data-test="s">S</div>}
        toc={<div data-test="t">T</div>}
      >
        <div data-test="m">M</div>
      </AppShell>,
    )
    expect(container.querySelector('[data-test="h"]')).not.toBeNull()
    expect(container.querySelector('[data-test="s"]')).not.toBeNull()
    expect(container.querySelector('[data-test="t"]')).not.toBeNull()
    expect(container.querySelector('[data-test="m"]')).not.toBeNull()
  })

  it('omits sidebar/toc when not provided', () => {
    const { container } = render(<AppShell header={<header />}><main /></AppShell>)
    expect(container.querySelector('.awt-app-shell__sidebar')).toBeNull()
    expect(container.querySelector('.awt-app-shell__toc')).toBeNull()
  })
})
```

- [ ] **Step 2: Run to fail**

Run: `npx vitest run packages/layout/components/__tests__/AppShell.test.tsx`
Expected: FAIL

- [ ] **Step 3: CSS**

```css
/* packages/layout/css/app-shell.css */
.awt-app-shell {
  display: grid;
  grid-template-columns: minmax(200px, 240px) 1fr minmax(200px, 240px);
  grid-template-rows: auto 1fr;
  grid-template-areas:
    "header  header  header"
    "sidebar main    toc";
  min-height: 100vh;
  background: var(--color-surface);
  color: var(--color-text-primary);
}
.awt-app-shell--no-toc { grid-template-columns: minmax(200px, 240px) 1fr; grid-template-areas: "header header" "sidebar main"; }
.awt-app-shell--no-sidebar { grid-template-columns: 1fr minmax(200px, 240px); grid-template-areas: "header header" "main toc"; }
.awt-app-shell--no-sidebar.awt-app-shell--no-toc { grid-template-columns: 1fr; grid-template-areas: "header" "main"; }
.awt-app-shell__header  { grid-area: header; position: sticky; top: 0; z-index: 5; }
.awt-app-shell__sidebar { grid-area: sidebar; }
.awt-app-shell__main    { grid-area: main; padding: 1.5rem 2rem; overflow: auto; }
.awt-app-shell__toc     { grid-area: toc; padding: 1.5rem 1rem; }

@media (max-width: 900px) {
  .awt-app-shell, .awt-app-shell--no-toc, .awt-app-shell--no-sidebar {
    grid-template-columns: 1fr;
    grid-template-areas: "header" "main";
  }
  .awt-app-shell__sidebar, .awt-app-shell__toc { display: none; }
}
```

- [ ] **Step 4: Component**

```tsx
// packages/layout/components/AppShell.tsx
import type { ReactNode } from 'react'

export type AppShellProps = {
  header: ReactNode
  sidebar?: ReactNode
  toc?: ReactNode
  children: ReactNode
}

export function AppShell({ header, sidebar, toc, children }: AppShellProps) {
  const cls = [
    'awt-app-shell',
    !sidebar && 'awt-app-shell--no-sidebar',
    !toc && 'awt-app-shell--no-toc',
  ]
    .filter(Boolean)
    .join(' ')
  return (
    <div className={cls}>
      <div className="awt-app-shell__header">{header}</div>
      {sidebar && <div className="awt-app-shell__sidebar">{sidebar}</div>}
      <main className="awt-app-shell__main">{children}</main>
      {toc && <div className="awt-app-shell__toc">{toc}</div>}
    </div>
  )
}
```

- [ ] **Step 5: Export**

Append:

```ts
export { AppShell, type AppShellProps } from './components/AppShell'
```

- [ ] **Step 6: Run to pass**

Run: `npx vitest run packages/layout/components/__tests__/AppShell.test.tsx`
Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add packages/layout
git commit -m "feat(layout): AppShell composing header/sidebar/main/toc"
```

### Task 2.8: Layout examples in `site/`

**Files:**
- Create: `site/examples/layout/index.tsx`
- Modify: `site/src/manifest.ts`

- [ ] **Step 1: Create the example**

```tsx
import {
  SiteConfigProvider,
  ContentProvider,
  RouteProvider,
  type SiteConfig,
  type SiteEntry,
} from '@agentic-cookbook/agentic-web-toolkit/site-model'
import {
  AppShell,
  HeaderConnected,
  SidebarConnected,
  TableOfContents,
  Breadcrumbs,
} from '@agentic-cookbook/agentic-web-toolkit/layout'
import { useState } from 'react'

import '@agentic-cookbook/agentic-web-toolkit/layout/css/base.css'
import '@agentic-cookbook/agentic-web-toolkit/layout/css/app-shell.css'
import '@agentic-cookbook/agentic-web-toolkit/layout/css/header.css'
import '@agentic-cookbook/agentic-web-toolkit/layout/css/sidebar.css'
import '@agentic-cookbook/agentic-web-toolkit/layout/css/breadcrumbs.css'
import '@agentic-cookbook/agentic-web-toolkit/layout/css/toc.css'

export const meta = { id: 'layout', label: 'Layout' }

const config: SiteConfig = {
  branding: { title: 'Layout Demo', titleEmphasis: 'The' },
  meta: { description: '', siteUrl: '' },
  hero: { heading: 'Layout', body: '' },
  nav: {
    sections: [
      { key: 'guides', label: 'Guides', path: '/guides' },
      { key: 'api', label: 'API', path: '/api' },
    ],
  },
}

const entries: SiteEntry[] = [
  {
    slug: '/guides/install',
    section: 'guides',
    raw: '',
    html: '',
    headings: [
      { id: 'overview', text: 'Overview', depth: 2 },
      { id: 'requirements', text: 'Requirements', depth: 2 },
      { id: 'macos', text: 'macOS', depth: 3 },
      { id: 'linux', text: 'Linux', depth: 3 },
    ],
    frontmatter: { title: 'Install' },
  },
  {
    slug: '/guides/configure',
    section: 'guides',
    raw: '',
    html: '',
    headings: [],
    frontmatter: { title: 'Configure' },
  },
  {
    slug: '/api/auth',
    section: 'api',
    raw: '',
    html: '',
    headings: [],
    frontmatter: { title: 'Authentication' },
  },
]

function Inner({ pathname, navigate }: { pathname: string; navigate: (p: string) => void }) {
  return (
    <div className="awt-layout awt-example-content">
      <AppShell
        header={<HeaderConnected onSearchOpen={() => alert('Search!')} />}
        sidebar={<SidebarConnected onNavigate={navigate} />}
        toc={<TableOfContents headings={entries.find((e) => e.slug === pathname)?.headings ?? []} />}
      >
        <Breadcrumbs trail={pathname === '/' ? [{ label: 'Home', path: '/' }] : [
          { label: 'Home', path: '/' },
          ...pathname.replace(/^\//, '').split('/').map((seg, i, arr) => ({
            label: seg.charAt(0).toUpperCase() + seg.slice(1),
            path: '/' + arr.slice(0, i + 1).join('/'),
          })),
        ]} />
        <h1 id="overview" style={{ color: 'var(--color-accent)' }}>{pathname}</h1>
        <p>Pick a sidebar item to navigate. The TOC, breadcrumbs, and active sidebar item update.</p>
        <h2 id="requirements">Requirements</h2>
        <p>Some text.</p>
        <h3 id="macos">macOS</h3>
        <p>Some text.</p>
        <h3 id="linux">Linux</h3>
        <p>Some text.</p>
      </AppShell>
    </div>
  )
}

export default function LayoutExample() {
  const [pathname, setPathname] = useState('/guides/install')
  return (
    <SiteConfigProvider config={config}>
      <ContentProvider entries={entries}>
        <RouteProvider pathname={pathname} hash="" navigate={setPathname}>
          <Inner pathname={pathname} navigate={setPathname} />
        </RouteProvider>
      </ContentProvider>
    </SiteConfigProvider>
  )
}
```

- [ ] **Step 2: Register in manifest**

Edit `site/src/manifest.ts`:

```ts
import type { ComponentType } from 'react'
import * as Chat from '../examples/chat'
import * as ReferenceWebSite from './examples/reference-web-site'
import * as SiteModel from '../examples/site-model'
import * as Layout from '../examples/layout'

export type ExampleEntry = {
  id: string
  label: string
  Component: ComponentType
}

export const examples: ExampleEntry[] = [
  { ...Chat.meta, Component: Chat.default },
  { ...SiteModel.meta, Component: SiteModel.default },
  { ...Layout.meta, Component: Layout.default },
  { ...ReferenceWebSite.meta, Component: ReferenceWebSite.default },
]
```

- [ ] **Step 3: Run dev server and visually verify**

Run: `cd site && npm run dev` (background)

Open the site, click "Layout":
- The 3-column AppShell renders: header at top, sidebar on the left, main in middle, TOC on the right
- Clicking sidebar items changes the visible breadcrumbs and the active item highlight
- The TOC lists Overview, Requirements, macOS, Linux. Scrolling the main column moves the active highlight in the TOC
- Switching themes via the rail's theme picker re-skins everything (sidebar background, accent color, borders)

Stop dev server.

- [ ] **Step 4: TypeScript check**

Run: `cd site && npx tsc --noEmit`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add site/examples/layout site/src/manifest.ts
git commit -m "feat(site): add layout demo example"
```

### Task 2.9: Phase 2 wrap-up

- [ ] **Step 1: Full test + typecheck**

Run: `npm test && npx tsc --noEmit`
Expected: all green

- [ ] **Step 2: Push**

```bash
git push
```

---

## Phase 3 (sketch — separate plan): `packages/controls/`

Out of scope for this plan. Future plan covers:

- **Files to create:**
  - `packages/controls/package.json`, `index.ts`
  - `packages/controls/components/SearchDialog.tsx` (modal shell, prop-driven; consumes `useSearchState` from site-model)
  - `packages/controls/components/SearchDialogConnected.tsx` (reads `useContent`, `useCurrentRoute` to wire navigation)
  - `packages/controls/components/ThemeToggle.tsx` (move from `src/theme/ThemeToggle.tsx` — keep `src/theme/ThemeProvider.tsx` where it is)
  - `packages/controls/css/search-dialog.css`, `theme-toggle.css`
- **Tests:** SearchDialog rendering smoke + key handling integration; ThemeToggle click-cycles
- **Examples:** `site/examples/controls/index.tsx` demonstrating both controls
- **Constraints:** Same theming rules; CSS uses `var(--color-*)` exclusively; no Tailwind utility classes

## Phase 4 (sketch — separate plan): `packages/content/`

Out of scope for this plan. Future plan covers:

- **Files to create:**
  - `packages/content/package.json`, `index.ts`
  - `packages/content/components/HomePage.tsx` (prop-driven hero + section grid; connected reads `useSiteConfig`+`useContent`)
  - `packages/content/components/SectionIndex.tsx` (prop-driven section card grid; connected variant)
  - `packages/content/components/MarkdownView.tsx` (extract from `DocPage` — renders `entry.body` HTML safely)
  - `packages/content/components/EntryCard.tsx`, `SectionCard.tsx` (extracted card primitives)
  - `packages/content/css/home-page.css`, `section-index.css`, `cards.css`, `markdown.css`
- **Tests:** Card rendering smoke; HomePage with mocked entries; MarkdownView passes-through HTML
- **Examples:** `site/examples/content/index.tsx`
- **Constraints:** Same theming rules; consume markdown.css from `src/theme/markdown.css` for prose typography

---

## Self-Review

**Spec coverage check (Phase 1 + 2 only — Phases 3/4 are sketches):**

- ✅ Three-layer architecture (data / view-model / UI) — Phase 1 establishes layers 1 + 2; Phase 2 starts layer 3
- ✅ Each new export demoed in `site/` — Tasks 1.14 (site-model) and 2.8 (layout)
- ✅ All component CSS uses `var(--color-*)` / `var(--font-*)` tokens, no literal colors, no Tailwind utility classes — Tasks 2.2–2.7 CSS files
- ✅ `reference-web-site` package not modified — only new packages created; manifest added new examples without removing existing ones
- ✅ Dual API per component (prop-driven + connected) — Tasks 2.3, 2.4, 2.5, 2.6 each create both variants
- ✅ Router abstraction via `LinkProvider`/`useLink` and `RouteProvider`/`useCurrentRoute` — Tasks 1.10, 1.11
- ✅ Search state separable from UI — Task 1.12 `useSearchState`
- ✅ All public exports added to root `package.json` exports map — Tasks 1.1, 2.1
- ✅ Tests for every pure function and provider; rendering smoke tests for components — every component task has a step-1 test
- ✅ Frequent commits — every task ends with a commit step

**Placeholder scan:** None — every code step contains complete code.

**Type consistency check (verified against `packages/reference-web-site/types.ts`):**
- `NavNode` is `{ label: string; path: string; domain?: string; children: NavNode[] }`. Sidebar, nav.ts, and SidebarConnected all use these exact fields.
- `SiteEntry` is `{ slug; section; subsection?; domain?; raw; html; headings; frontmatter }`. Display titles come from `entry.frontmatter.title`. All test fixtures and example entries construct the full shape.
- `HeadingEntry` is `{ id; text; depth }`. TableOfContents uses `depth` and a `minDepth`/`maxDepth` props pattern instead of a per-heading `hidden` flag.
- `slugToBreadcrumbs` returns `[]` for `/` and does not prepend "Home"; consumers prepend Home themselves if desired.
- `BreadcrumbEntry` defined in `breadcrumbs.ts` and re-exported via `site-model/index.ts`.
- `SearchIndex` / `SearchResult` defined in `search.ts`, consumed by `useSearchState.ts`.
- `RouteValue` defined in `RouteProvider.tsx`, consumed by `BreadcrumbsConnected.tsx`, `SidebarConnected.tsx`, `TableOfContentsConnected.tsx`.
- `LinkComponent` defined in `LinkProvider.tsx`, consumed by `Breadcrumbs.tsx`, `Header.tsx`, `Sidebar.tsx`.
